# Test profile — overrides for integration tests
# Most properties are injected dynamically by AbstractIntegrationTest via @DynamicPropertySource
spring:
  main:
    allow-bean-definition-overriding: true
  flyway:
    enabled: false
  jpa:
    open-in-view: false

# Override MyBatis-Plus to use H2-compatible settings
mybatis-plus:
  mapper-locations: classpath*:mapper/**/*.xml
  type-aliases-package: com.mall.module.*.entity
  global-config:
    db-config:
      logic-delete-field: deleteStatus
      logic-delete-value: 1
      logic-not-delete-value: 0
  configuration:
    default-enum-type-handler: org.apache.ibatis.type.EnumNameTypeHandler

management:
  endpoints:
    web:
      exposure:
        include: health,info

mall:
  auth:
    token-header: Authorization
    token-head: "Bearer "
    jwt-secret: "test-secret-key-for-integration-testing-32chars-at-least!"
    jwt-expiration-seconds: 3600

# MinIO — test stub
minio:
  endpoint: http://localhost:9999
  bucket-name: test
  access-key: minioadmin
  secret-key: minioadmin

logging:
  level:
    com.mall: DEBUG
    org.testcontainers: INFO
